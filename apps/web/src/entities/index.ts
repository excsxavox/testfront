/**
 * Clientes API ligeros y tipos DTO alineados al contrato REST.
 * Re-export mínimo del paquete compartido para validar el workspace.
 */
export type { PilotoServiceId } from "@piloto/shared-types";
