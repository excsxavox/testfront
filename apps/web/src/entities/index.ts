/**
 * Clientes API ligeros y tipos DTO alineados al contrato REST.
 * Re-export del paquete compartido para validar el workspace.
 */
export type { PilotoApiHealthResponse, PilotoServiceId } from "@piloto/shared-types";
