/**
 * Valor por defecto documentado para el piloto (TZ del establecimiento).
 * La lectura real desde env/DB vivirá en `infrastructure/config` cuando exista persistencia.
 */
export const DEFAULT_HOTEL_TIMEZONE = "Europe/Madrid" as const;
